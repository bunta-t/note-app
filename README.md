# note-app

本文入力欄は CodeMirror 6 (CDN経由・ESM import) を使用しているため、`index.html` を `file://` で直接開くことはできません。プロジェクトフォルダで簡易サーバーを起動してから開いてください。

```
python -m http.server 8000
```

ブラウザで `http://localhost:8000` を開きます。